{
    IBClasses = (
        {
            ACTIONS = {makeDonation = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainWindow = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}